name: Retag nginx image
on:
  push:
    branches:
      - ikenna

jobs:
  retag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Retag to ikenna
        run: |
          docker pull nginx:alpine
          docker tag nginx:alpine codnam007/nginx:ikenna
